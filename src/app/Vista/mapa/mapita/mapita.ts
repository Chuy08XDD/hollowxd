import { Component } from '@angular/core';
import { CommonModule } from '@angular/common';

@Component({
    selector: 'app-mapita',
    standalone: true,
    imports: [CommonModule],
    templateUrl: './mapita.html',
})
export class Mapita{}